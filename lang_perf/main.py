from cython_class import Solver
import sys
import numpy as np

# s = Solver()
# adj_mat = np.array([[1, 3], [0, 2], [1, 3], [0, 2]])
# s.init(adj_mat)
#
# a = s.all_pairs_shortest_path()
print("bla")

# s = Solver()
# s.init(sys.argv[1])
#
# if len(sys.argv) >= 3 and sys.argv[2] == "full":
#     s.compute_hash_full()
# else:
#     s.compute_hash()
